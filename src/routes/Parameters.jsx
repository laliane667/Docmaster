import { useEffect, useState } from "react"
import { Paper, Card, Group, Stack, Text, Title, rem, Divider, Button, Pagination, Center, Badge } from "@mantine/core"

export default function Parameters() {
  const [activePage, setPage] = useState(0)

  return (

    <Paper>

    </Paper>

  )
}
